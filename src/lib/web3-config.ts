import { getDefaultConfig } from '@rainbow-me/rainbowkit';\nimport {\n  mainnet,\n  polygon,\n  base,\n  polygonMumbai,\n  baseSepolia,\n} from 'viem/chains';\n\nconst projectId = process.env.NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID || '';\n\nif (!projectId) {\n  console.warn(\n    'WalletConnect Project ID not set. Please set NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID in .env.local'\n  );\n}\n\nexport const wagmiConfig = getDefaultConfig({\n  appName: 'Web3Video',\n  projectId,\n  chains: [\n    mainnet,\n    polygon,\n    base,\n    polygonMumbai,\n    baseSepolia,\n  ],\n  ssr: true,\n});\n\n// Network configuration\nexport const SUPPORTED_NETWORKS = [\n  {\n    id: polygon.id,\n    name: 'Polygon',\n    rpcUrl: process.env.NEXT_PUBLIC_POLYGON_RPC_URL || 'https://polygon-rpc.com',\n    explorer: 'https://polygonscan.com',\n  },\n  {\n    id: base.id,\n    name: 'Base',\n    rpcUrl: process.env.NEXT_PUBLIC_BASE_RPC_URL || 'https://mainnet.base.org',\n    explorer: 'https://basescan.org',\n  },\n  {\n    id: polygonMumbai.id,\n    name: 'Mumbai (Testnet)',\n    rpcUrl: 'https://rpc-mumbai.maticvigil.com',\n    explorer: 'https://mumbai.polygonscan.com',\n  },\n  {\n    id: baseSepolia.id,\n    name: 'Base Sepolia (Testnet)',\n    rpcUrl: 'https://sepolia.base.org',\n    explorer: 'https://sepolia.basescan.org',\n  },\n];\n\n// Contract addresses\nexport const CONTRACT_ADDRESSES = {\n  membership: (process.env.NEXT_PUBLIC_MEMBERSHIP_CONTRACT_ADDRESS ||\n    '0x0000000000000000000000000000000000000000') as `0x${string}`,\n  adEngine: (process.env.NEXT_PUBLIC_AD_ENGINE_CONTRACT_ADDRESS ||\n    '0x0000000000000000000000000000000000000000') as `0x${string}`,\n};\n